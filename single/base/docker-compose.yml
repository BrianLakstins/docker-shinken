version: "3.5"
services:
  shinken-base:
    build: ./2.4
    image: shinken-base:2.4
  shinken-base-latest:
    build: ./2.4
    image: shinken-base:latest

#This docker-compose file is for building only
#>docker-compose build