version: "3.5"
services:
  shinken-minimal:
    build: ./latest
    image: shinken-minimal:latest
    restart: unless-stopped
    expose:
      - 7768    
      - 7769
      - 7770  
      - 7771
      - 7772
    ports:
      - 7767:7767   
      - 5667:5667 
    volumes:
      - type: volume
        source: shinken-config
        target: /etc/shinken
        volume:
          nocopy: false    
      - type: volume
        source: shinken-data
        target: /var/lib/shinken
        volume:
          nocopy: false     
      - type: volume
        source: shinken-log
        target: /var/log/shinken
        volume:
          nocopy: false                    

volumes:
  shinken-config:
  shinken-data:   
  shinken-log:     